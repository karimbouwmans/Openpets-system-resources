{
  "name": "openpets-system-resources",
  "version": "1.5.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "OpenPets plugin: live CPU, RAM, and optional host-provided GPU and system-volume metrics to the left of the pet.",
  "scripts": {
    "test": "node test.js && node scripts/package-catalog.test.js",
    "package:catalog": "node scripts/package-catalog.mjs"
  },
  "devDependencies": {
    "@open-pets/plugin-sdk": "^3.3.0"
  }
}
